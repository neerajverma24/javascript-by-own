// Blog- 12 ======================================================================================================

/*

// Check if element is exist in an array ? 

let a = [1, 2, 3, 4, 5];

// create a function to identify if the element in the given array is exist or not..
// here, we are going to use 'indexOf() method' because it returns -1 , if the element is not found..

// for example :- 
// console.log(a.indexOf(6)); // output : -1


// now we are to create a function

function checkElement(a, e) {              
     // here, a represents array and e represent elements...

    let b = a.indexOf(e);        // it will show the index value of the number..
    if (b !== -1) {
        console.log(`Element ${e} exists on : ${b}`)
    } else {
        console.log("Element does not exits");
    }

}
checkElement(a, 3);     // output : Element 3 exists on : 2  , where 2 is a index value...

*/




